.claude/
.DS_Store

# Python
__pycache__/
*.py[cod]
backend/.pytest_cache/
backend/.storage/
backend/bocra_dev.db
backend/venv/
backend/.env

# Local frontend dependencies/build output are already covered by frontend/.gitignore,
# but keeping the most important entries here helps before the first commit.
frontend/node_modules/
frontend/.next/

# Backend-only branch can safely ignore any local frontend leftovers.
frontend/
